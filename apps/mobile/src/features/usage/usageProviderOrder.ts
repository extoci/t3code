import type { UsageProviderKind } from "@t3tools/contracts";

/** Bottom-to-top provider order for usage charts and matching table columns. */
export const PROVIDER_ORDER: readonly UsageProviderKind[] = ["codex", "claude"];
